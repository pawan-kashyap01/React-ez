import React from 'react';
import ReactDOM from 'react-dom/client';
const name = 'Pawan'
const jsxHeadingElement = <h1 id="headinginjsx">Heading in jsx. eval{}</h1> 
const userInput = '<script>alert("XSS");</script>';
//React Element
const headingElement = (
    <h1 id="heading"> This is react element </h1> 
)
//React Compoenent
const HeadingComponent1 = ()=>{
    return <h1>This is heading 1</h1>
}
const HeadingComponent2 = ()=>{
    return <h1>This is heading 2</h1>
}

const number = 12345;
const HeadingComponent = ()=>(
    <div id='Container'>
        {userInput}
        {number*2} //Writing js expressions in curly braces
        {jsxHeadingElement} // jsx element in in componet
        {headingElement} // React element in componet
        <HeadingComponent1/>
        {HeadingComponent2()}
        <h1>This is body</h1>
    </div>
    )

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<HeadingComponent/>);